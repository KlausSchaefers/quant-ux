/**
 * As we do not have multiple inheritence in here, we cascade the the inheritence down
 *
 * Controller -> SVgController -> DesignToken ->
 * Widget -> Responsive -> Snapp -> Screen -> CopyPaste -> Group -> 
 * Layer -> Templates ->BaseController
 */
import AIController from './AIController'

export default class Controller extends AIController {

}
/**
 * As we do not have multiple inheritence in here, we cascade the the inheritence down 
 * 
 * Controller -> Widget -> Screen -> CopyPaste -> Group -> Layer -> Templates ->BaseController
 */
import Widget from 'canvas/controller/Widget'

export default class Controller extends Widget {

}